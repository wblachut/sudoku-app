import styled from 'styled-components';

export const CellInputDiv = styled.div`
	& input {
		overflow: hidden;
		width: 100%;
		background: none;
		outline: none;
		border: none;
		color: white;
		text-align: center;
		font-size: 1.8vmax;
		min-height: 50px;
	}
`;
